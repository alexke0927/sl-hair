# SL百元專業剪髮系統 - 健檢與優化報告

這份報告紀錄了 `WEB` 資料夾內目前的架構問題、Bug 以及未來的優化建議，供日後開發與維護時參考。

## 🔴 已發現的 Bug (Bugs)

### 1. 首頁 (`index.html`) 的致命語法錯誤
* **問題描述**：在 `index.html` 約第 1497 行的 `<script>` 區塊中，`initButtonRipples` 函式結尾多了一個多餘的 `}` 大括號。這個 SyntaxError 會導致瀏覽器停止執行該區塊的所有 JavaScript。
* **嚴重影響**：首頁的「手機版漢堡選單無法點擊展開」、「導覽列往下滑動時不會出現毛玻璃背景」、「按鈕點擊水波紋特效」等功能目前是完全失效的狀態。
* **修復方案**：刪除該多餘的大括號。（已於本次對話中順手幫您修復）

### 2. 跨裝置無法同步 (LocalStorage 實體隔離)
* **問題描述**：系統的所有排隊資料（當前叫號、等待人數）皆存放在 `localStorage` 中。這意味著資料是綁定在「當下這台機器的這個瀏覽器」上的。
* **嚴重影響**：客人掃描 QR Code 用自己的手機打開網頁時，手機的瀏覽器內並沒有店裡平板的資料，因此客人的手機永遠只能看到「目前叫號：0」，無法達到遠端追蹤的效果。
* **修復方案**：若要讓客人用手機追蹤，必須建置真實的後端系統（例如串接 Firebase Realtime Database 或是 Supabase），透過雲端即時同步數字，取代 `localStorage`。

### 3. 幽靈檔案 (`app.js` 與 `style.css`)
* **問題描述**：專案目錄下雖然有 `app.js` 與 `style.css` 兩個檔案，但經過全域搜尋，發現沒有任何一個 HTML 網頁有引入它們。
* **嚴重影響**：這兩個檔案目前淪為無用的孤兒檔案，而真正的程式碼全部被直接寫死在 HTML 內部。

---

## 🟡 架構優化與重構建議 (Optimizations)

### 1. 違反 DRY (Don't Repeat Yourself) 原則
* **現狀**：`index.html`、`queue.html`、`admin.html` 三份檔案各自擁有一大坨高達數百行的 `<style>`。其中許多 CSS 變數（如品牌色彩 `--bg-main`）、按鈕樣式、導覽列樣式都是一模一樣的重複代碼。
* **優化建議**：應該將共用的外觀樣式統一移至 `style.css`，並在三個網頁中透過 `<link rel="stylesheet" href="style.css">` 引入。這樣未來如果想要將「百元剪髮」的品牌色從藍色改成黑色，只要改一行程式碼，三個網頁就會同時生效，大幅降低維護成本。

### 2. 前端資料遺失風險
* **現狀**：因為店長後台 (`admin.html`) 資料全靠瀏覽器記憶，如果店員不小心按到了「清除瀏覽資料」或「清除 Cookie」，或是瀏覽器處於無痕模式，整間店當天的排隊資料與累計服務人數會瞬間人間蒸發。
* **優化建議**：在尚未全面升級成雲端資料庫前，建議在後台加入一個「下載今日營業報表」的按鈕，讓店長可以在打烊時將當天的服務人數下載成 Excel 或純文字檔備份。

### 3. 未發出號碼的防呆邏輯
* **現狀**：在此次檢修前，候位時間計算器允許客人輸入尚未發出的號碼（例如已發到 15 號，客人卻輸入 99 號）。
* **優化建議**：目前已經在 `queue.html` 中加入了防呆阻擋，未來若開發新功能（例如線上預約），所有使用者輸入的數字皆需經過嚴格的極值檢驗。
